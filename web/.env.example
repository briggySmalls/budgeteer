# Google OAuth client ID (Web application) for the Sheets/Drive integration.
# Copy this file to `.env.local` (gitignored) and fill in your client id.
# OAuth client IDs are public — they ship in the client bundle — so this is not a secret.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
